#!/bin/bash

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
. "${SCRIPT_DIR}/env_common"

was_running=0
if "${SCRIPT_DIR}/main" status >/dev/null 2>&1; then
    was_running=1
fi

write_oh_my_sage_env

if [ "${was_running}" -eq 1 ]; then
    "${SCRIPT_DIR}/main" stop
    "${SCRIPT_DIR}/main" start
fi
